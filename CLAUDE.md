# CLAUDE.md - 项目规则与约定

## 项目概述

`D:\Code\.prompt` 是一个 **AI 提示词与解决方案存储库**，用于集中管理和分类存储各类 AI 提示词及问题解决方案，方便复用与维护。

---

## 核心工作流（每次会话必须遵循）

### 阶段一：会话前 — 查找

每次与 AI 对话开始时，**必须先到 `D:\Code\.prompt` 查找**是否有相关的提示词或解决方案：

1. 根据用户问题提取关键词
2. 在 `D:\Code\.prompt` 中搜索匹配的 `prompt.md` 和 `solution.md`
3. 若找到相关内容，读取并整合到回复中，避免重复造轮子
4. 若未找到，正常进行对话

### 阶段二：会话后 — 存储

AI 回复 / 对话结束后，**必须评估是否需要存储**高质量内容：

1. **提示词存储**：对话中产生的优质提示词 → `{类型}/{标题}-{YYYYMMDD}/prompt.md`
2. **解决方案存储**：解决的技术问题 → `解决方案/{类型}/{问题标题}-{YYYYMMDD}/solution.md`
3. 存储前确认内容质量：结构清晰、可直接复用、无冗余
4. 存储后更新下方「已建立的提示词分类」表格

---

## 目录命名规范

### 第一级子文件夹：提示词类型

- 命名格式：`{提示词类型}`
- 示例：`前端`、`后端`、`设计`、`运维`、`测试`、`通用`
- 根据提示词所属的领域或技术栈分类

### 第二级子文件夹：具体提示词

- 命名格式：`{总结提炼标题}-{日期时间}`
- 日期时间格式：`YYYYMMDD`（如 `20260328`）
- 标题应简洁、能概括提示词核心内容
- 示例：`高级UI设计规范-20260328`、`亚克力与液态玻璃效果-20260328`

### 目录结构示例

```
D:\Code\.prompt\
├── 前端\
│   ├── 高级UI设计规范-20260328\
│   │   └── prompt.md
│   └── 亚克力与液态玻璃效果-20260328\
│       └── prompt.md
├── 设计\
│   └── {标题}-{日期}\
│       └── prompt.md
├── 解决方案\
│   └── {类型}\
│       └── {问题标题}-{日期}\
│           └── solution.md
└── ...
```

---

## 解决方案目录规范

### 结构

```
解决方案/{类型}/{问题标题}-{YYYYMMDD}/solution.md
```

### 规则

- **第一级** `{类型}`：问题所属领域，如 `前端`、`后端`、`设计`、`运维`、`DevOps` 等
- **第二级** `{问题标题}-{YYYYMMDD}`：简洁概括问题，标题应能一眼看懂问题内容
- 文件统一命名为 `solution.md`
- 文件头部包含：问题描述、环境信息、根因分析
- 内容结构：问题 → 原因 → 解决方案 → 验证方式

---

## 提示词文件规范

- 每个子文件夹下的提示词文件统一命名为 `prompt.md`
- 文件头部应包含：适用平台/场景、核心原则等元信息
- 内容应结构化，使用 Markdown 标题分级组织
- 提示词应可直接复制使用，语言清晰、指令明确

---

## 拆分存储规则

当收到一份综合性提示词时：

1. **先存储完整整合版**：将原文整理后整体存入 `{类型}/{综合标题}-{日期}/prompt.md`
2. **再拆分为子模块**：按主题/功能点拆分，各自存入独立子文件夹
3. 拆分后的子模块之间内容不重叠，各司其职
4. 每个子模块可作为独立提示词单独使用

---

## 会话记录规则

每次对话结束后，执行以下操作：

1. **存储高质量内容**：将有价值的提示词和解决方案存入对应目录
2. **更新索引表**：新增内容后更新下方「已建立的提示词分类」和「已建立的解决方案」表格
3. **记录会话要点**：将对话中的关键信息记录到记忆文件，包括：
   - 新增或修改的文件
   - 确认的规则和约定
   - 用户的偏好和反馈
   - 待办或未完成的事项

---

## 已建立的提示词分类

| 类型 | 提示词 | 日期 |
|------|--------|------|
| 前端 | 高级UI设计规范（完整整合版） | 20260328 |
| 前端 | 亚克力与液态玻璃效果 | 20260328 |
| 前端 | 高级动效设计 | 20260328 |
| 前端 | 渐变配色方案 | 20260328 |
| 前端 | 布局设计规范 | 20260328 |
| 前端 | 粒子与微元素效果 | 20260328 |
| 前端 | AI前端提示词方法论（完整整合版） | 20260412 |
| 前端 | 设计系统Prompt架构 | 20260412 |
| 前端 | AI前端工具Prompt实践 | 20260412 |
| 前端 | AI前端常见错误与避坑 | 20260412 |
| 前端 | 设计风格图鉴（93风格·理论md+实践demo+styles.json·人机双用） | 20260704 |
| 前端 | 前端设计专业名词（术语词典·编写精准Prompt用） | 20260704 |
| 后端 | AI后端开发Prompt方法论（API/数据库/认证/工作流） | 20260412 |
| 后端 | 后端架构与项目结构规范 | 20260412 |
| 后端 | 安全Checklist与避坑指南 | 20260412 |
| 通用 | 开发环境配置指南（Git/环境变量/Docker） | 20260412 |
| 通用 | 部署与维护指南（Vercel/Railway/CI-CD） | 20260412 |
| 通用 | Idea到MVP全链路工作流 | 20260412 |
| 通用 | 开发工具环境清单（版本/路径/未装参考） | 20260619 |
| 通用 | Claude Skills与Agents清单（41 skill + 13 agent） | 20260619 |
| 设计 | 系统化UI设计体系（完整整合版） | 20260328 |
| 设计 | 苹果HIG按钮层级体系 | 20260328 |
| 设计 | 经典设计原则体系 | 20260328 |
| 设计 | 原子设计方法论 | 20260328 |
| 设计 | 8pt网格与色彩体系 | 20260328 |
| 设计 | DESIGN.md设计系统格式规范 | 20260405 |
| 设计 | 知名网站设计系统参考目录（55个网站索引） | 20260405 |
| 设计 | Apple设计系统（电影级产品展示） | 20260405 |
| 设计 | Vercel设计系统（黑白工程精确） | 20260405 |
| 设计 | Stripe设计系统（紫色金融优雅） | 20260405 |
| 设计 | Linear设计系统（暗色开发者原生） | 20260405 |
| 设计 | Spotify设计系统（暗色沉浸音乐） | 20260405 |
| 设计 | Notion设计系统（温暖极简纸感） | 20260405 |
| 设计 | Figma设计系统（多彩工具面板） | 20260405 |
| 设计 | BMW设计系统（工业零圆角） | 20260405 |
| 设计 | Supabase设计系统（暗色翡翠开源） | 20260405 |
| 设计 | Raycast设计系统（macOS精密暗色） | 20260405 |

### 设计系统参考来源

- 完整 55 个网站的 DESIGN.md 存放于 `D:\Code\.prompt\awesome-design-md-main\design-md\`
- 涵盖 AI、开发者工具、基础设施、设计工具、金融科技、消费品牌等类别
- 需要其他网站的设计系统时，从该目录获取完整 DESIGN.md

### 设计风格图鉴说明

- 93 种前端设计风格，按 6 大流派分册：网页表面(20)/氛围主题(33)/沉浸式3D(8)/移动端App(14)/桌面软件(10)/复合(8)
- 每种风格配套：理论(精髓/配色/字体/视觉特征/复刻要点/即用Prompt) + 实践(可视化demo，配色点击复制 hex)
- 数据源 `styles.json` 机器可读，AI 可直接引用字段复刻
- 位置：`D:\Code\.prompt\前端\设计风格图鉴-20260704\`
- 入口：该目录 `README.md`（索引）→ `复刻实战指南.md`（三步法/模板/混搭/避坑）

## 已建立的解决方案

| 类型 | 问题标题 | 日期 |
|------|----------|------|
| 通用 | Claude Skills安装与管理（npx skills工具） | 20260619 |
| 通用 | Windows开发工具环境探测与梳理 | 20260619 |
